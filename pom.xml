<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.8.1</version> <!-- Use the appropriate version -->
            <configuration>
                <source>11</source> <!-- Set to the lowest common Java version (Java 11) -->
                <target>11</target> <!-- Set to the lowest common Java version (Java 11) -->
            </configuration>
        </plugin>
    </plugins>
</build>
